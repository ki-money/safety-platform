# Core web framework
Flask==2.3.3

# Database
pymongo==4.8.0
dnspython==2.6.1

# Security / hashing
Werkzeug==2.3.7

# Scientific / analytics stack (ai_analytics.py)
numpy==1.24.4
scipy==1.10.1

# HTML sanitisation
bleach==6.1.0

# Optional – nice to have for dev / production
gunicorn==22.0.0          # production WSGI server
python-dotenv==1.0.1      # for loading .env (if you use MONGO_URI, etc.)

# Development / testing (remove for production if you want a minimal install)
pytest==8.3.2